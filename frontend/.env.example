PORT=3001
JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=development
VITE_API_URL=/api

# For Vercel deployment:
# 1. Go to Vercel Dashboard → Project Settings → Environment Variables
# 2. Add JWT_SECRET with your secret value
# This will automatically be available to your API functions

# Optional: Override Horizon / Soroban endpoints (useful for testnet or custom RPC)
# If you set these in Vercel, the frontend will use them. Defaults point to Stellar Testnet.
VITE_HORIZON_URL=https://horizon-testnet.stellar.org
VITE_SOROBAN_RPC=https://soroban-testnet.stellar.org
